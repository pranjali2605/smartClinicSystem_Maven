spring.application.name=smartclinic-api
server.port=9000

# =========================
# MySQL Database Config
# =========================
# Choose ONE of these (delete the other)

# Option 1:
spring.datasource.url=jdbc:mysql://localhost:3306/Records
spring.datasource.username=root
spring.datasource.password=tiger

# Option 2:
# spring.datasource.url=jdbc:mysql://localhost:3306/data
# spring.datasource.username=root
# spring.datasource.password=#1268SQL$

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# =========================
# MongoDB (for history logs)
# =========================
spring.data.mongodb.uri=mongodb://localhost:27017/smartclinic_history

# =========================
# Thymeleaf
# =========================
spring.thymeleaf.cache=false

# =========================
# Spring Security
# =========================
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN
